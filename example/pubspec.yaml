name: stop_watch_timer_example
description: Demonstrates how to use the stop_watch_timer plugin.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=2.17.0 <3.0.0'

dependencies:
  cupertino_icons: ^1.0.1+1
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

  stop_watch_timer:
    path: ../

flutter:
  uses-material-design: true
